# Backend

A minimal Go backend for mtranscribe.

## Running

```bash
go run main.go
```

The server will start on port 8080.

## Endpoints

- `GET /` - Returns a welcome message
- `GET /health` - Health check endpoint
