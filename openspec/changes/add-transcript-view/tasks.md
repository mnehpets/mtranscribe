# Tasks: Add Transcript View

- [ ] Create `frontend/src/components/TranscriptView.vue` with basic structure.
- [ ] Implement `getSpeakerColor` logic in `TranscriptView.vue`.
- [ ] Implement the rendering loop for `transcript.turns`.
- [ ] Implement display of `transcript.title` and collapsible `transcript.notes`.
- [ ] Apply styling: bold speaker names, layout on separate lines.
- [ ] Create unit tests in `frontend/src/components/__tests__/TranscriptView.test.js` (or `.ts` if configured) to verify rendering and speaker coloring.
- [ ] Verify visually with a demo data set.
