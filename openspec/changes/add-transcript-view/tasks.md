# Tasks: Add Transcript View

- [x] Create `frontend/src/components/TranscriptView.vue` with basic structure.
- [x] Implement `getSpeakerColor` logic in `TranscriptView.vue`.
- [x] Implement the rendering loop for `transcript.turns`.
- [x] Implement display of `transcript.title` and collapsible `transcript.notes`.
- [x] Apply styling: bold speaker names, layout on separate lines.
- [x] Create unit tests in `frontend/src/components/__tests__/TranscriptView.test.js` (or `.ts` if configured) to verify rendering and speaker coloring.
- [x] Verify visually with a demo data set.
